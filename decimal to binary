#include<stdio.h>
int main()
{
    int rem,num,i=0,a;
    int bi=0;
    scanf("%d",&num);
    a=1/2;
    printf(" a is %d",a);
    while(num!=0)
    {
        rem=num%2;
        bi+=rem*pow(10,i);
        num=num/2;
        i+=1;

    }
    printf("binary equivalent number is %d",bi);
}
